<twig:Popover:Item href="" :isGranted="false" type="{{ isGranted ? 'submit' : false }}" disabled="{{ not isGranted }}"
    {{ ...attributes.without('isGranted') }}>
    <form method="post" action="{{ href }}">{{ parent() }}</form>
</twig:Popover:Item>
