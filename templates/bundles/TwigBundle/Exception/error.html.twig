{% extends 'error.html.twig' %}

{% set errorTitle = 'Oops… something happened.' %}
{% set errorDescription = 'This wasn’t supposed to happen, but we’re on it.' %}

{% block errorButtons -%}
    {{ parent() }}
    <button class="btn btn-secondary btn-transparent" data-button-action="retry">
        {{ ux_icon('lucide:refresh-cw') }}
        <span>Retry</span>
    </button>
{%- endblock %}
